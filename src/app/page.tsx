import Button from "@/components/ui/Button";
import { db } from "@/lib/db";
import { FC } from "react";

const Home: FC = async ({}) => {
  return (
    <div>
      <Button>Hello</Button>
    </div>
  );
};

export default Home;
